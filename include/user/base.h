#ifndef _USER_BASE_H
#define _USER_BASE_H

// User functions are declared here - stored in memory (no filesystem for now)

// default idle and return functions
void idle();
void on_return();

// some user functions
void first_func();
void second_func();
void third_func();

#endif // _USER_BASE_H